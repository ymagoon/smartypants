function refreshForm(innerHTML) {
  const newReviewForm = document.getElementById('new_review');
  newReviewForm.innerHTML = innerHTML;
}

function addReview(reviewHTML) {
  const reviews = document.getElementById('jsreview');
  reviews.insertAdjacentHTML('beforeend', reviewHTML);
}

<% if @review.errors.any? %>
  refreshForm('<%= j render "reviews/form", review: @review, bundle: @bundle   %>');
<% else %>
  addReview('<%= j render "reviews/review", review: @review %>');
  refreshForm('<%= j render "reviews/form", review: Review.new, bundle: @bundle %>');
<% end %>
